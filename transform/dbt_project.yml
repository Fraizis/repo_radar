name: repo_radar
version: "1.0.0"
config-version: 2

profile: repo_radar

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  repo_radar:
    staging:
      +materialized: view
    gold:
      +materialized: table
      +engine: "MergeTree()" 

